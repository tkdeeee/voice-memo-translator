import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourdomain.voicememo',
  appName: 'voice-memo-app',
  webDir: 'build'
};

export default config;
